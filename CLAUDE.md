# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ⚠️ Ground rule — always confirm before touching files

**Always ask the user for confirmation before creating a new file or editing an existing file.** State what you intend to change and wait for an explicit go-ahead first — do not create or modify files proactively, even for changes that look trivial. Reading, searching, and analyzing the codebase need no confirmation.

## What this is

A Flask + PostgreSQL dashboard for managing water/dam monitoring data (projects, river basins, stations, sensors, cameras, samplings, areas, teams, users). Server-rendered Jinja templates driven by a jQuery/Tabulator frontend. UI strings and many code comments are in Mongolian/Thai; the maintainer communicates in Mongolian.

## Bundled toolchain (Windows)

The repo vendors its own interpreter and database — **use them, don't assume a system install**:

- Python: `python313\python.exe` (all commands below assume this)
- PostgreSQL server: `pgsql\` (bundled, runs on port **5444**)
- **Never modify** `pgsql\`, `python313\`, or `static\js\libs\` (vendored third-party). Do JS work in `static\js\` (not `libs`). One agreed exception: the AI detector's packages (torch CUDA cu128, torchvision, ultralytics, scipy, pandas, seaborn, setuptools) are installed into `python313` — the list lives in `ai/requirements.txt`; install nothing else there.

## Commands

- **Run the app:** `app_run.bat` (or `python313\python.exe app.py`). Serves on port 88 (gevent `WSGIServer` in prod; Flask dev server with reloader when `APP_DEBUG=True`).
- **Start/stop DB:** `pgsql_start.bat` / `pgsql_stop.bat`.
- **Migrate DB:** `migrate_db.bat` runs `flask db migrate` + `flask db upgrade` in one shot (auto-inits `migrations/` if missing). Manual: set `FLASK_APP=app.py`, then `python313\python.exe -m flask db migrate -m "..."` and `... db upgrade`.
- **Format + lint:** `pre-commit.bat` runs Black then Flake8, both excluding `pgsql,python313,dist` and the vendored `ai/yolov5`; Flake8 uses `--max-line-length=150 --extend-ignore=E203`. Match these when writing code.
- **Regenerate package imports:** after adding/removing a file in `models/` or `routes/`, run `generate_init.bat` (`generate_init.py`). It rewrites each `__init__.py` with `__all__` and the imports (`from .<m> import *` for models, `from .<m> import <m>_bp` for routes). Editing those `__init__.py` files by hand will be overwritten.
- **Load default data (fixtures):** `load_fixtures.bat` (or `flask seed`) runs `fixtures/loader.py::load_fixtures()`, which reads `fixtures/data/*.json` and inserts them **idempotently** (skips rows that already exist by their natural key, so user edits are never overwritten). This also runs automatically at app startup (`run_startup_checks`). To add seed data, drop a `NN_<name>.json` file in `fixtures/data/` — format `{"model": <ModelName>, "unique_by": <field>, "records": [...]}`; the numeric prefix controls load order. Records are built via the model constructor, so property setters run (e.g. `User.Password` is hashed). This replaced the old per-model `create_default_*` seeders.
- **No test suite exists.** There is no build step (static assets are committed).

Config comes from a `.env` file (see `README.md`): `DB_USERNAME/PASSWORD/HOST/PORT/NAME`, `APP_SECRET_KEY`, `APP_DEBUG`. `config.py` builds `SQLALCHEMY_DATABASE_URI` from those.

## Architecture

### App wiring
`app.py` → `create_app()` is the factory: applies `ProxyFix`, loads `config.Config`, installs a `CustomJSONProvider` (dates → strings, `ensure_ascii=False`), then `db.init_app`, `Migrate`, JWT, Flask-Login, `register_handlers`, `register_blueprints`. `run_startup_checks` verifies the DB connection and seeds a default `User` and `Settings` before serving. `db` is a bare `SQLAlchemy()` in `database.py` (import from there, not from a Flask extension object). **Keep `app.py` minimal** — it only wires components together and starts the server; setup and helper logic (logging, login manager, JSON provider, the startup banner, etc.) lives in `extensions.py` (or the relevant module), never inlined in `app.py`.

### The "module" convention (most important pattern)
Each feature is a **module** name (`stations`, `sensors`, `cameras`, `samplings`, `teams`, `users`, `projects`, `riverbasins`, `filetransfer`, `http`, `httplog`, `csvlogger`, `stationdata`, `eventlog`) wired in **four parallel places**. (The former `areas` module was folded into the sensor's Flow tab per design rev.22: `SensorFlowConfigures` holds the surveyed cross-section; `util/hydro.py` computes the level → wetted-area Profile — filled by the Flow tab's "Calculate" button (`POST /api/sensors/profile`), never overwritten on save — and `compute_flow()`; on every inbound payload `StationData.apply_flow` derives `FLOW`/`AREA` = A(WL)·k(WL)·VELOCITY for the Flow sensor mapped on the station's Water configures "Flow Rate" row (checked + text = SensorID), unless the device already sent FLOW.)

| Layer | Location |
|-------|----------|
| Model | `models/<module singular>.py` |
| API routes | `routes/<module singular>.py` → blueprint `<name>_bp`, mounted at `/api/<module>` in `urls.py` |
| Templates | `templates/dashboard/<module>/` — `index.html` (Tabulator list) + `form.html` + `form-<tab>.html` |
| Frontend JS | `static/js/dashboard/<module>.js` |

`urls.py::register_blueprints` maps blueprints to `/api/<module>` prefixes. `templates/dashboard/index.html` dynamically includes `dashboard/<Module>/index.html` and loads `static/js/dashboard/<Module>.js` from the `Module` route param. Adding a module = create all four, add it to the `api_list` in `urls.py`, then run `generate_init.py`.

**Shared/common dashboard templates go in `templates/dashboard/main/`.** Any page or partial that is used across modules or generically (shared macros, reusable form snippets, common includes) belongs there — not inside a specific module's folder. Reference them as `dashboard/main/<file>.html` (e.g. `{% from "dashboard/main/config_field.html" import render_field with context %}`).

**Reviewing or changing a module means checking its whole file set, not a single file.** When asked to look at / fix / improve a module — e.g. "check `user.py`" or "the users module" — always review all of the module's parallel files together, because a change in one usually implies changes in the others:
- `models/<module>.py`
- `routes/<module>.py`
- `urls.py` (blueprint registration + page routing)
- `static/js/dashboard/<module>.js` **or** `static/js/modules/<module>.js`
- `templates/dashboard/<module>/*` **or** `templates/modules/<module>/*`

The `dashboard` vs `modules` split is the admin dashboard vs the public/front site; a module may have files under either or both.

### Model conventions
Models subclass `db.Model` and expose **classmethods that hold the business logic** — `save(params)`, `delete(params)`, `list(params)`, `getData(id)`, plus an instance `serialize()`. Class-level `sort`, `searchFields`, `required_fields` drive generic list/search/validation. `save()` handles both insert and update by iterating `__table__.columns` generically; JSONB columns are detected and JSON-parsed via `_parse_json_fields`. `list()` returns `{data, sort, last_row, last_page, filtered}` plus Tabulator column defs assembled in the route. Routes stay thin: merge `request.args`/JSON/form into one dict and delegate to the model classmethod, returning `jsonify(result), result["Code"]`.

### Frontend conventions
`static/js/project.js` holds the **generic** form engine shared by every module: `updateEditForm` (populates a form from a serialized record, including `.app-json-data[data-field]` JSONB blocks), `serializeEditForm`, and `initTabulators`. Per-module JS files (e.g. `stations.js`) open a Bootbox dialog with the cloned `.form-tmp`, wire `ajaxForm` to `/api/<module>/save`, and in `beforeSerialize` collect `.app-json-data` blocks into JSON payloads. A global `LOCAL_VARIABLES` carries `Authorization` (incl. `UserType`) and `StaticText`; `pagePermission` arrays gate pages by `UserType`.

### `util/statictext.py` — single source of truth for ALL text (critical rule)
**Never hard-code user-facing text — labels, messages, button captions, dropdown options, icons, field titles, response messages — anywhere in Python, Jinja templates, or JavaScript. Always add it to `util/statictext.py` and reference it from there.** This is a core convention of the project; string literals scattered in code/markup are treated as a mistake to fix.

What lives here: per-model field labels (`<Model>Field`), form tab definitions (`<Model>FormTab`), nested form schemas (`StationConfigures`, `WaterConfigures`, `APIConfigures`, `FTPConfigures`, `Sensor*Configures`, `AreaConfigures`, …), dropdown option maps (`Regions`, `UserTypes`, `SensorTypes`, `API_Protocols`, …), `Messages`, `ResponseCode`, `Icon` (HTML icon snippets), `StatusLabel`, `Export`, `Images`, and app constants (`APP_NAME`, `APP_COLOR`, …). Reuse existing keys by cross-referencing (e.g. `CameraFormTab` reuses `CameraField["CameraConfigures"]`) rather than repeating a literal.

How each layer consumes it:
- **Backend (Python):** `from util import statictext`, then `statictext.Messages["SuccessSaved"]`, `statictext.ResponseCode[code]`, `statictext.StationField["SiteCode"]`, etc. Routes/models return these, never inline English/Thai strings.
- **Templates (Jinja):** the value is passed in as `StaticText=statictext` and used as `StaticText.Icon.Save | safe`, `StaticText.StationFormTab`, `StaticText.Save`, etc. Templates loop over these dicts to render forms, so adding a field/tab/config is an edit here, not new HTML.
- **Frontend (JS):** `routes/main.py::/init` reflects the whole `statictext` module to JSON (`vars(statictext)` minus a few filesystem-path keys in `excluded_keys`). `static/js/project.js` fetches `/main/init` once into `LOCAL_VARIABLES.StaticText` and caches it in `localStorage` (key = the `app` attribute). JS reads strings as `LOCAL_VARIABLES.StaticText.Messages.LettersOnly`, `LOCAL_VARIABLES.StaticText.Icon['-']`, `LOCAL_VARIABLES.StaticText.RequiredField`, `LOCAL_VARIABLES.StaticText.UserTypes[type]`, etc.

Consequences to remember: (1) everything in `statictext.py` except the `excluded_keys` paths is shipped to the browser and cached client-side — **never put secrets, tokens, or absolute filesystem paths there**; add non-public constants elsewhere or to `excluded_keys`. (2) Because `LOCAL_VARIABLES.StaticText` is cached in `localStorage`, a newly added string may not appear until the cached copy is refreshed (the `/init` fetch on load / clearing storage) — expect that when a new key "doesn't show up."

### Auth
Flask-Login sessions (`login_view = /dashboard/login`) plus a JWT manager. `UserType`: 1 Administrator, 2 Supervisor, 3 Staff, 4 Guest. The frontend `pagePermission` arrays only hide pages — **authorization is enforced server-side** with `util/auth.py`:
- `@require_types(*ADMINS | *MANAGERS | *EDITORS)` on every endpoint that changes data (users/settings → Administrator; projects/river basins/teams → Administrator+Supervisor; stations/sensors/cameras/samplings/areas/services/event actions → +Staff; Guest is read-only). Users may edit only their own profile (`can_manage_user`).
- **Station scope**: Staff/Guest only see their teams' stations. `Team.Stations` (JSONB list of StationIDs, formerly `RiverBasins`) drives `Team.scope_station_ids()` (None for managers, `[-1]` = nothing when the user has no team — fail-closed), `Team.apply_station_scope(requestData)` for list/counter endpoints and `Team.can_access_station(id)` for get/detail/action endpoints. Applied to stations, projects/river basins (derived), http, file transfer, csv loggers (via their transfer), http logs, event logs and station data.

### Background worker & services (`services/`)
`app.py` starts one daemon thread (`services/scheduler.py::start_worker`) right after the startup checks (only in the reloader child in debug). It ticks every 5 s, runs each registered job inside its own app context / DB session, and swallows job errors so the web server is never affected. `WORKER_ENABLED` (Settings) switches it off. Jobs:
- `http_sender` — delivers `HttpLog` rows with Status 0 (Queue). Rows are queued by `StationData.ingest` → `HttpLog.enqueue_for_station` the moment a device posts data (design: "when new data hits our DB"); payload built by `util.build_http_payload` from the HTTP service's Parameter Mapping; GET → query string, POST/PUT → JSON or `key=value` body; retries per `RetryAttempts`/`RetryDelay`; full URL, request, response code/body kept on the row (Status 1 Sent / 2 Failed, `HttpLogStatuses`).
- `csv_logger` — every `LogInterval` minutes (clock-aligned) appends a row to `RTU Data/<SiteCode>/csv/<LoggerID>/<FilenameFormat>` and uploads it via the logger's File Transfer (`services/file_transfer.py`, stdlib `ftplib`, FTP/FTPS explicit+implicit, directory structure); outcome in `CsvLogger.LastRun/LastResult`.
- `event_watcher` — Security cameras drop `<IP>_<ch>_<yyyymmddHHMMSS>_<EVENT>.jpg` into `Camera.eventWatchPath` (`RTU Data/_security_in`); files become `EventLog` rows (camera matched by RSTP/Onvif IP), images move to `EventLog.drfFilePath` (`static/data/events/<yyyymm>/`, served from `EventLog.filePath`), unparseable files to `_unmatched/`. Public page `/events` (templates/modules/events, static/js/modules/events.js) with approve/reject.
- `image_uploader` — files under `Camera.images_out_folder()` (`RTU Data/<SiteCode>/<CameraID>/images_out/`) are sent through the camera's Upload JPG (FTP) settings; delivered files move to `sent/`; outcome in `Camera.LastUploadRun/LastUploadResult`. Folder paths are class attributes / model helpers (like `Station.drfFilePath`), not Settings rows.

**Runtime folders** (all git-ignored): `tmp/` is only for chunked front-end uploads (`*.partN` + assembled file; `/main/cleartmp` removes leftovers older than 24 h). `RTU Data/` (`statictext.APP_DATA_PATH`, overridable with `RTU_DATA_PATH` in `.env`; the legacy server used the same folder name) holds private worker files that must never be web-served, laid out per station: `RTU Data/<SiteCode>/csv/<LoggerID>/`, `RTU Data/<SiteCode>/<CameraID>/images|images_temp|images_out/`, shared `RTU Data/_security_in/`, and `RTU Data/_unassigned/<CameraID>/` for cameras without a station (`Station.data_folder_for()`, `Camera.data_folder()` and the `images_*_folder()` helpers). Deleting a camera (`Camera.remove()`) removes its RTU Data folder, its public pictures and its model file. Anything the public front must show (approved event images, camera animations) goes under `static/data/`.
- `snapshot` — every `SNAPSHOT_INTERVAL_MINUTES` (Settings, 0 = off) downloads each enabled camera's ISAPI picture (`Camera.build_links()["SnapshotURL"]`, Basic/Digest auth) to `Camera.images_folder()` as `<CameraID>_<yyyymmddHHMMSSffffff>.jpg` (legacy timestamped naming, archive trimmed to the newest `SNAPSHOT_KEEP_COUNT` files, Settings, 0 = keep all) and copies the newest one to the public `static/data/cameras/<CameraID>/image.jpg`. The animation `image.gif` in the same folder is **not** made by this job: the AI worker writes its RTSP clip frames to `images_temp/1..N.jpg` (`AI_FRAME_COUNT`) and animates them with `ai/animation.py::build_animation` (Pillow, 640 px, 1 s/frame). `Camera.snapshot()` exposes `SnapshotImage` (gif when present, else image.jpg, else the blank picture), `SnapshotStill` and `SnapshotTime` (from the newest archive file name); the URLs carry `?t=<mtime>` and `util/handlers.py` adds `Cache-Control: no-store` under `/static/data/cameras/`. The front CCTV card opens the picture full size in a modal (`cctv.js` lightbox, no download button); the camera form previews it. At most `MAX_PER_TICK` cameras per tick and one attempt per camera per interval (unreachable cameras cost `TIMEOUT_SECONDS` each and never block the other jobs for long).
- `sysinfo` — every 10 s samples CPU (Windows `GetSystemTimes` / Linux `/proc/stat`) and refreshes cached runtime-folder sizes; `services/sysinfo.py` also reads memory, disk, and PostgreSQL size/tables with stdlib + `ctypes` only (no psutil — `python313/` is read-only).
- `ai_worker` — every `AI_WORKER_INTERVAL_MINUTES` (Settings, default 15, 0 = off; clock-aligned slots, one camera per tick) processes each enabled `CameraType = Sensor` camera that has an uploaded model file (`Camera.model_file()`) **in a separate interpreter** (`services/ai_worker.py::run_script` → `AI_PYTHON` from `.env`, else the running one; OpenCV/torch never run inside the gevent web process). Stage 1 `ai/grab.py`: `AI_CLIP_SECONDS` RTSP clip → `RTU Data/<SiteCode>/<CameraID>/raw_temp/raw.mp4` + `raw.json` (fps, frame times), `AI_FRAME_COUNT` evenly spaced frames → `images_temp/1..N.jpg`. Stage 2 `ai/detect.py` (port of the legacy `get_waterlevel_newdata` + `detect_movement`): YOLO gauge box on frame 1, its bottom row = waterline, every Sampling table linked to the camera (`Sensor.Meta.WaterLevels.configs.WaterLevel` rows, optional `Points` crop + `LevelFrom`) → cubic spline → the lowest level wins (legacy multi-gauge rule); `ai/velocity.py` optical flow over the frames inside the Velocity sensor's `Points` region scaled by its `Length` (m) and the real frame spacing → velocity, direction, dominant colour (`#rrggbb` + legacy name), rain flag; the annotated frames (green box, red waterline, level) become `static/data/cameras/<CameraID>/image.gif` + `image.jpg`. Stage 3: frame 1 archived as a snapshot, one `StationData` row with `DeviceID = CameraID`, `RecordTime` = clip time, `Data.DateTime` = the 15-min slot (legacy CSV rule), `Data` keys chosen by the station's Water configures rows (Point 1/2 → `WL`/`WL2` when that sensor's Water Level table names the camera; the `Velocity` row's sensor supplies the region/length and yields `VELOCITY`, `DIRECTION`, `COLOR`, `RAIN_FLAG` together, like the legacy CSV columns), `FLOW`/`AREA` via `StationData.apply_flow`, `Raw` = the whole AI result (+ `image` = archived frame relative to RTU Data, clip metadata), then `HttpLog.enqueue_for_station` like an inbound payload. No gauge → nothing stored. Device and camera rows coexist for one station (the map takes the newest). Per-camera outcome in `ai_worker.STATUS`; failures are logged and never block the other jobs. `DIRECTION`/`COLOR`/`RAIN_FLAG` are text/flag keys: grid columns only, charts keep their explicit numeric key lists.
- `retention` — once a day, batch-deletes old rows so the fact tables stay bounded: `DATA_RETENTION_DAYS` (station data), `RAW_RETENTION_DAYS` (clears `StationData.Raw` only), `HTTPLOG_RETENTION_DAYS`, `EVENTLOG_RETENTION_DAYS` (+ image files); file rules `CSV_RETENTION_DAYS` (`data/csv`), `SENT_IMAGE_RETENTION_DAYS` (`data/images_out/*/sent`) and abandoned `tmp/` uploads older than 24 h (`purge_tmp_uploads`, also behind the Administrator-only `/main/cleartmp`); Settings rows, `0` = keep forever. `tbl_station_data` is the system's fact table (all measurements); composite indexes `(StationID, RecordTime)` / `(StationID, EventTime)` / `(Status, NextAttempt)` cover the time-series and queue scans. Table partitioning is not needed until tens of millions of rows.

The scheduler keeps per-job statistics (`STATS`: last run, duration, result/error, run + error counts, next run) exposed by `scheduler.status()`.

**Dashboard overview** (`/dashboard`, `templates/dashboard/main/index.html` + `static/js/dashboard/main.js`, labels in `statictext.DashboardSummary` / `WorkerJobs`): one `GET /main/summary` every 30 s — station status counts + "needs attention" list (`Station.live_status`, the same rule as the map), payloads today / last 24 h per hour, HTTP delivery and security-event counters, all station-scoped via `Team`; Administrators also get the worker job table (next-run countdown) and system tiles (uptime, CPU, memory, disk, DB size/tables, folder sizes). Charts use the vendored ECharts (`static/js/libs/echarts/dist/echarts.min.js`, loaded in `templates/main/footerjs.html`); Chart.js is vendored but unused. Two front-end gotchas: (1) ECharts (any UMD library) **must be included before the ArcGIS script** — the ArcGIS Dojo AMD loader defines `define.amd`, so a UMD file loaded after it registers as an anonymous AMD module (no global) and corrupts the loader (the map's `require()` then fails silently); (2) the theme's `dist/css/style.css` redefines Bootstrap's `.fs-5` as **5px** — never use `fs-5` for visible numbers. ECharts needs a `<div>` container, not a `<canvas>`. **Chart tooltips (any chart, any page) must not exceed 12px**: every ECharts `tooltip` option carries `textStyle: { fontSize: 12 }` (dashboard `main.js`, `stationdata.js`, front `station.js`, `statistics.js`); keep that on new charts.

**Station Data page** (`/dashboard/stationdata`, `templates/dashboard/stationdata/`, `static/js/dashboard/stationdata.js`, labels `statictext.StationDataField` / `StationDataRanges` / `StationDataBuckets` / `StationDataParameters`): read-only grid of `tbl_station_data` with the agreed `Data` columns flattened (`V_<key>`), filters Project / River Basin / Station / quick period (`Range`) or custom dates / Parameter + Min-Max (numeric JSONB filter via `StationData.value_expr`), summary tiles (`/api/stationdata/summary`) and an ECharts hydrograph of one station (`/api/stationdata/series` with `parameters: [...]` → `StationData.series_multi`, one locked `bucket` auto | raw | hour | day for all series, raw up to `MAX_RAW_POINTS`; level on the left axis, flow / velocity on right axes, rain as bars hanging from the top, wetted area deliberately excluded). NB: `routes/stationdata.py` defines a view named `list`, so use `builtins.list` there. All three endpoints share `StationData._filtered_query` and the Team station scope.

**Public front** (`templates/modules/<page>/index.html` + `static/js/modules/<page>.js`, labels `statictext.FrontPage`; pages other than Home require login): map popup shows the newest values + "More" (`project.js` `buildPopupContent`); `/station/<id>` = station detail (`POST /api/stations/public` → `Station.public_detail`: identity, Point 1/2 specifications, newest values + status, rain accumulated 1/2/3 hydrological days ending 07:00 via `StationData.rain_accumulation`) + 7-day chart; `/cctv[/<id>]` = station list + camera snapshots (`POST /api/stations/cameras` → `Station.with_cameras`, cameras matched by `Camera.Meta.CameraConfigures.configs.StationID`, no credentials); `/statistics[/<id>]` = one chart per parameter (`/api/stationdata/series`) with CSV/print; `/report` = payload grid (`/api/stationdata/list`) with export; `/events` = Event Log. Header notifications (`templates/main/notification.html` + `static/js/notifications.js`, `POST /api/eventlog/latest`) list the newest pending security events. **Type scale** (dashboard and front alike): 12px content, 11px secondary/labels, big figures only in KPI tiles (`.kpi-value`, `.stat-tile-value`); front rules live under `body[app-main]` in `custom.css`, page headings use `.page-title`, counters `.stat-tile`, list actions are icon buttons with `title` tooltips. Public map rows (`Station.live_status`) never include `Meta`. The station page (`/station/<id>`) also shows the station's cameras, its recent security events, the cross-section (`damchart.js`) with the live level, and a measurement section (period filter + `hydrochart.js` hydrograph + summary tiles + a Tabulator of `/api/stationdata/list` fed by `cfg-extra-params`). `static/js/hydrochart.js::renderHydroChart()` is the shared hydrograph used by the dashboard Station Data page and the front.

**PDF export** (`project.js::exportPdfWithImages`, the PDF button of every Tabulator): jsPDF + autoTable in landscape A4 with **Sarabun** (OFL, `static/fonts/`, fetched once and registered in the document VFS by `ensurePdfFont`) because the vendored `arial-font.js` has neither Thai glyphs nor usable metrics. Exports only `download !== false` columns, strips HTML from cells, embeds images only when the `<img>` really loaded and guards `addImage`. Never pass `minCellHeight: undefined` to autoTable — it turns every row height into NaN and all rows collapse onto one line.

Related modules/endpoints: `POST /api/inbound/<DeviceID>` (device-facing REST API server, gated by `Station.Meta["API"]`: status, source allow-list, Basic/Bearer/API-Key auth, Inbound Data Mapping → `StationData` with `Data` mapped + `Raw`), `/api/stationdata` (read-only), `/api/httplog` (list/counters/get), `/api/eventlog` (list/counters/get/action). Public map status (`/api/stations/sites`) comes from the newest `StationData` per station vs the StationConfigures thresholds; older than `DATA_TIMEOUT_MINUTES` → "No connection". TLS certificate uploads for the REST API tab live in `certs/<StationID>/` (git-ignored, never web-served). Settings rows seeded by fixtures: `DATA_TIMEOUT_MINUTES`, `WORKER_ENABLED` (shown as a switch; `statictext.BooleanSettings`).

**Filters**: every list / report page builds its filter bar from `templates/dashboard/main/filter_field.html` (`filter_select`, `filter_input`, `filter_period`, `filter_dates`, `filter_param_range`; labels in `statictext.FilterLabels`, look in `custom.css` `.app-filters`). The Station filter cascades from Project / Watershed through `static/js/filters.js::cascadeStationFilter()`. The map's own filter bar (`main/map.html`) is separate and must stay as it is.

Config-field extras understood by `templates/dashboard/main/config_field.html` + `project.js`: `type` file / generate / checkbox / color (native picker, `#rrggbb`), `show_when` (conditional visibility, AND of listed fields), `help`, table `column_options` (select column). Camera RTSP/ISAPI links are generated by `Camera.build_links()` (and previewed by `renderCameraLinks` in `cameras.js`).

### AI water-level detection (`ai/`)
`ai/` is the clean re-implementation of the legacy `xserver_new.py` water-level server (the legacy `wl-detection/` folder was removed once everything it held - models, sampling tables, camera list - had been imported): YOLOv5 finds the gauge/water box on a camera frame, the bottom row of the box touching the water (smallest `y2`) is the waterline, and the Sampling table (`Meta.SamplingConfigures.configs.CameraConfigures = [{x: pixel row, y: level m}]`) turns that pixel into a level through a cubic spline (`ai/calibration.py`, extrapolated at both ends). **Inference never runs inside the web process**: `ai/yolov5/` (vendored YOLOv5 `models/` + `utils/`) and the pickled checkpoints use the package name `models`, which would shadow the dashboard's own `models/` — so the dashboard calls `python313\python.exe ai\detect.py job.json` as a subprocess and parses the last stdout line (`{"ok", "y", "y_crop", "bbox", "conf", "level", "in_range", "image", ...}`; see `ai/README.md`). `ai/detector.prepare_runtime()` restores `torch.load(weights_only=False)` and aliases `pathlib.PosixPath` on Windows. Weights are uploaded per camera on the camera form (`statictext.CameraModelField`, `POST /api/cameras/modelupload` / `modeldelete` → `Camera.store_model` / `remove_model`) and stored as `ai/trained_models/<CameraID>.pt` (git-ignored; only the file name is kept in `Meta.CameraConfigures.configs.TrainedModel`). The generic file config field (`config_field.html` type `file`) gains a remove button whenever the field declares a `remove` route. Velocity sensors carry the detection region (`Points`) plus its real-world width `Length` (m) for the future optical-flow job. Real station data (TC.04, TC.12, TTC.01, C.12, TS.KB, TM03, TMB.01: stations, cameras, samplings, sensors, Water mapping) lives only in the database (no fixtures); the five stations other than TC.04 / TC.12 carry approximate names / coordinates flagged in their Remark.

## Working style (from the former Copilot rules)
- **Write all code comments in English.** Even though some existing comments (and all UI text via `statictext.py`) are in Mongolian/Thai, every new or edited source comment — in Python, JS, Jinja, and batch files — must be in English.
- Read the existing files for a module before changing it; mirror the established pattern and naming.
- Prefer small, targeted edits over rewrites; avoid adding dependencies.
- When a change touches routes + models + templates + DB together, note the cross-layer impact before editing.
